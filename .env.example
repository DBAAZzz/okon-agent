# Environment Variables Setup
# Each app manages its own .env file in its directory

# Agent (apps/agent/.env)
DEEPSEEK_API_KEY=your_deepseek_api_key

# Web (apps/web/.env.local) - currently no env vars needed
