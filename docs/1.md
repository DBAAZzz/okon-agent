# Minimal DeepSeek Ink TUI (Vercel AI SDK)

## 1) Install dependencies

```bash
pnpm install
```

## 2) Set API key

```bash
cp .env.example .env
# edit .env and fill your key
```

## 3) Start TUI

```bash
pnpm ask
```

在终端里直接输入问题，模型会流式增量输出。  
输入 `exit` 或 `quit` 退出。
